'use client';

import { useEffect } from 'react';
import '@/lib/amplify';

export function ConfigureAmplifyClientSide() {
  useEffect(() => {
    // Amplify configuration is loaded from @/lib/amplify
  }, []);

  return null;
}